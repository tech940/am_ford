/**
 * UsedConfidence has been retired in the editorial redesign.
 * CPO trust signals are conveyed through the Inventory Reveal section
 * (condition label on each vehicle) and the Trade-In section copy.
 * This file is kept only to avoid breaking lazy imports during transition.
 */
export function UsedConfidence() {
  return null;
}
